<%= render 'header' %>

<div class="large-12 columns">
  <div class="large-4 columns">
    <h3>Edit Group</h3>
    <%= render 'form' %>
    <%= link_to 'Delete', @group, method: :delete, data: { confirm: 'Are you sure you want to delete this group?' } if @group.items.nil? %>
  </div>
</div>



